<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="002-create-transactions" author="aiylbank">
        <createTable tableName="transactions">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="source_account_id" type="BIGINT"/>
            <column name="target_account_id" type="BIGINT"/>
            <column name="amount" type="NUMERIC(19,2)">
                <constraints nullable="false"/>
            </column>
            <column name="type" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(10)">
                <constraints nullable="false"/>
            </column>
            <column name="balance_after" type="NUMERIC(19,2)"/>
            <column name="failure_reason" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="transactions"
                baseColumnNames="source_account_id"
                constraintName="fk_transactions_source_account"
                referencedTableName="accounts"
                referencedColumnNames="id"/>

        <addForeignKeyConstraint
                baseTableName="transactions"
                baseColumnNames="target_account_id"
                constraintName="fk_transactions_target_account"
                referencedTableName="accounts"
                referencedColumnNames="id"/>

        <sql>
            ALTER TABLE transactions
            ADD CONSTRAINT chk_transactions_amount
            CHECK (amount > 0);
        </sql>

        <sql>
            ALTER TABLE transactions
            ADD CONSTRAINT chk_transactions_type
            CHECK (type IN ('TRANSFER_OUT','TRANSFER_IN'));
        </sql>

        <sql>
            ALTER TABLE transactions
            ADD CONSTRAINT chk_transactions_status
            CHECK (status IN ('SUCCESS','FAILED'));
        </sql>

        <rollback>
            <dropTable tableName="transactions"/>
        </rollback>
    </changeSet>

</databaseChangeLog>